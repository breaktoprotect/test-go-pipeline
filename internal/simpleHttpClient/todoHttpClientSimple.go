package simplehttpclient

func main() {
	/* host := "jsonplaceholder.typicode.com"
	client := todos.New(host)

	newTodo := todos.Item{
		UserId:    1,
		Title:     "Example Todo",
		Completed: false,
	}

	response, err := client.CreateTodo(context.Background(), newTodo)
	if err != nil {
		fmt.Println(err)
		return
	}
	fmt.Println("New Todo created:", response) */
}
